import { useState } from "react";


export const prevState = ()=>{
    const[count, getCount] = useState(0);

    const increment = ()=>{
        // my state update call is based on prevState value
        
    }
}